on: [push, pull_request]
name: bootstrap
jobs:
  i386:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: prepare
        run:  git submodule update --init --recursive
      - name: run
        run:  ./rootfs.sh minikernel
